# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Allow local connections
local   all             all                                     trust

# Allow connections from any IP address with md5 password authentication
host    all             all             0.0.0.0/0               md5

# Allow IPv6 connections as well
host    all             all             ::/0                    md5

